// Highest number
// Write a program that asks for 10 numbers. Using logical operators and any other javascript functions/structures you've seen before, determine and output the highest of those numbers.

// const getNumbersList = prompt("Enter 10 numbers: (each separated by comma)");

// const arrNumbersStr = getNumbersList.split(',');

// const arrNumbers = arrNumbersStr.map(
//   (element) => Number(element)
// );

// arrNumbers.sort(
//   (a, b) => b - a 
// )

// console.log(`The highest number is: ${arrNumbers[0]}`);